{
  "name": "deep-clone",
  "version": "1.0.0",
  "description": "A native add-on for Node that deeply clones JavaScript objects using C++.",
  "main": "benchmark.js",
  "scripts": {
    "build": "node-gyp rebuild",
    "performance": "npm run build && node performance",
    "test": "npm run build && node test"
  },
  "author": "Aaron Arinder",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.5.0"
  },
  "gypfile": true,
  "devDependencies": {
    "benchmark": "^2.1.4",
    "lodash": "^4.17.19"
  }
}
